"""
Контент клиента: правила бренда, которые код обязан знать ТОЧНО.

ПОЧЕМУ ОТДЕЛЬНЫЙ МОДУЛЬ, А НЕ config.py:
config — это окружение и секреты (хост базы, ключи, токены). Они приходят
из .env и меняются от машины к машине. Здесь — контент клиента: он не секрет,
из .env не приходит и одинаков на любой машине этого клиента.

ПОЧЕМУ НЕ В knowledge_base:
там те же правила лежат ПРОЗОЙ ДЛЯ МОДЕЛИ — kb_018 говорит «завершуємо
від імені команди — «Команда «Медовий Ранок»»». Модели прозы достаточно,
коду — нет: гардрейлу нужна точная строка для сравнения, а не предложение
о ней. Два потребителя — два представления одного правила.
Дублирование осознанное, и замкнёт его scripts/validate_kb.py: он проверит,
что канон отсюда реально встречается в voice-чанке клиента. Пока эта связь
держится на дисциплине — правишь здесь, правь и в базе.

ЗДЕСЬ ПОДСТРОКИ, А НЕ РЕГУЛЯРКИ — сознательно. Регулярка решила бы задачу
границ слова одним символом (\\b), но этот файл редактирует клиент, а не
инженер: маркетолог кофейни должен уметь добавить сюда фразу, не выучив
синтаксис регулярных выражений. Цена решения — каждая строка обязана быть
безопасной подстрокой сама по себе (см. ловушку с «повернемо» ниже).

ГОРИЗОНТ:
сейчас один клиент = один деплой, поэтому это константы модуля. На втором
клиенте отсюда вырастает конфиг, который грузится по client_id — структура
(имя, подписи по языкам, списки фраз) уже разложена под это. Строить
абстракцию раньше нельзя: ось вариации станет видна только на втором клиенте,
а угаданная абстракция дороже дублирования.

ВСЕ СТРОКИ ЗДЕСЬ СРАВНИВАЮТСЯ С НОРМАЛИЗОВАННЫМ ЧЕРНОВИКОМ (guardrails.py:
casefold, снятие кавычек, схлопывание пробелов). Нормализация применяется
к ОБЕИМ сторонам, поэтому писать константы можно в естественном виде —
с кавычками и заглавными.
"""

# Имя бренда для промпта генерации (подставляется в SYSTEM_PROMPT_TEMPLATE).
#
# В английском промпте останется кириллицей — и это осознанно: как писать имя
# латиницей, модель узнаёт не отсюда, а из правил бренда (kb_025) и примера
# (kb_020), где канон задан точно. Дублировать транслитерацию здесь — значит
# завести второй источник истины для одной строки.
# Проверено на прогоне: rev_002 и rev_006 (английские) кириллицу в тело
# ответа не принесли.
BRAND_NAME = "Медовий Ранок"

# Канон подписи по языку ответа.
#
# Ровно та строка, которой обязан заканчиваться ответ. Источник в базе знаний:
# kb_018 (uk) и kb_025 (en, «spelled exactly this way»).
#
# Ключи совпадают с Language из state.py — язык ответа всегда равен языку
# отзыва, третьего варианта в системе нет.
SIGNATURES = {
    "uk": "Команда «Медовий Ранок»",
    "en": "The Medovyi Ranok team",
}

# Шаблонные фразы, запрещённые бренд-буком.
#
# ПРОВЕНАНС ВАЖЕН — здесь два разных класса строк:
#   1. Названо клиентом дословно: kb_015 «ваша думка дуже важлива для нас»
#      и kb_016 «your feedback is very important to us». Взято ядро фразы,
#      а не она целиком: модель может уронить «дуже», и точное совпадение
#      промахнётся.
#   2. Досеяно нами по классу «канцелярит» — бренд-бук говорит «без канцеляриту
#      та шаблонних фраз НА КШТАЛТ ...», то есть даёт пример класса, а не
#      закрытый список. Эти строки идут в чек-лист онбординга на подтверждение
#      клиентом.
#
# Чего здесь НЕТ намеренно: «дякуємо за ваш відгук». Фраза шаблонная, но kb_014
# прямо требует «Дякуємо за сигнал» на негативе — флаг бы спорил с бренд-буком
# и превратился бы в шум на каждой второй жалобе.
CLICHES = {
    "uk": [
        "важлива для нас",  # (1) ядро фразы из kb_015
        "приносимо вибачення за незручності",  # (2) досеяно
        "у найкоротші терміни",  # (2) досеяно
    ],
    "en": [
        "is very important to us",  # (1) ядро фразы из kb_016
        "we apologize for any inconvenience",  # (2) досеяно
        "we apologise for any inconvenience",  # (2) досеяно, британское написание
    ],
}

# Обещания, которые требуют РУЧНОГО действия оператора.
#
# Пайплайн промокоды не отправляет и деньги не возвращает — он пишет текст.
# Если черновик обещает гостю промокод (а kb_009/kb_010 велят обещать именно
# его), то после публикации кто-то обязан этот промокод реально прислать.
# Флаг превращает молчаливое обещание в строку «⚙️ Потрібна дія» на карточке.
#
# Ловит и обещания, которых в policy не было: проверяется ТЕКСТ ЧЕРНОВИКА,
# а не входные факты.
#
# ТОЧНОСТЬ ВАЖНЕЕ ПОЛНОТЫ. Оператор и так читает каждый черновик — пропущенный
# флаг стоит ему пары секунд, а ложный убивает доверие ко всем флагам сразу.
# Поэтому здесь только безопасные подстроки, без ложных друзей:
#   «безкоштовн» НЕ берём — совпало бы с «Wi-Fi безкоштовний» (kb_005)
#     и «заміна безкоштовна» (kb_003), а это факты, а не обещания. Зато полная
#     фраза «безкоштовний напій» из kb_009 безопасна: Wi-Fi не напиток.
#   «поверн» НЕ берём — совпало бы с «повернутись» і «поверніться», а kb_014
#     прямо велит запрашивать гостя вернуться.
#
# ЛОВУШКА, КОТОРУЮ ПОЙМАЛ ПРОГОН (rev_004). Здесь стояло голое «повернемо»
# с комментарием «омонимов нет». Омоним есть: украинское возвратное «-сь»
# приписывается прямо к этой форме, и «поверне[мо]сь до вас з відповіддю» —
# обещание ОТВЕТИТЬ, которого kb_014 требует на каждой жалобе, — давало
# ложный флаг. Поэтому глагол берётся только вместе с объектом: возврат денег
# от возврата к разговору отличает не глагол, а то, ЧТО возвращают.
#
# Цена этого решения названа честно: порядок слов в украинском свободен,
# и «повернемо вам кошти» здесь не поймается. Это осознанный пропуск —
# альтернатива (голая основа) даёт ложные срабатывания на фразе, которую
# бренд-бук предписывает.
ACTION_PHRASES = {
    "uk": [
        "промокод",
        "знижк",  # знижка / знижку / знижки
        "компенсац",  # компенсація / компенсацію
        "повернемо кошт",  # повернемо кошти
        "повернемо гроші",
        "повернемо вартість",
        "повертаємо вартість",  # формулировка самой kb_011
        "безкоштовний напій",  # формулировка kb_009
        "безкоштовного напою",
        "безкоштовним напоєм",
    ],
    "en": [
        "promo code",
        "discount",
        "refund",
        "free drink",  # «free of charge» из kb_004 сюда НЕ попадает
        "compensat",  # compensation / compensate
    ],
}